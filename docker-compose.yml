services:
  mkdocs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: "mkdocs"
    volumes:
      - "./docs:/docs"
      - "./mkdocs.yml:/mkdocs.yml"

    ports:
      - "80:8000"